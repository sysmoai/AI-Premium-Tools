# AIPT Pricing v2 control

The canonical AIPT pricing/access register is `ops/aipt-pricing-v2.json`.
Runtime commerce must fail closed if D1, API, checkout, or storefront state diverges from that approved register. Raw/legacy catalog files are historical inputs only and must never override the governed production projection.
